# Skill: nav-simplify
Simplify navigation and information architecture. Rules:
- Hero path: Home → Trade → Learn → Predictions → Portfolio (5 items max)
- Explore section: Options, Backtest, Market Intel, Arena (4 items max)
- Everything else under "More" collapsed by default
- Remove duplicate or near-duplicate pages
- Merge similar pages where possible
- Ensure every page is reachable from sidebar
- Mobile: bottom tab bar with 5 hero items only
Audit sidebar and routing, simplify. Report changes.
